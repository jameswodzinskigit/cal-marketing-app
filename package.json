{
  "name": "cal-marketing-app",
  "version": "1.0.0",
  "description": "CAL Marketing OS",
  "main": "server.js",
  "scripts": {
    "prestart": "node prestart.js",
    "start": "node server.js",
    "install-deps": "npm install"
  },
  "dependencies": {
    "googleapis": "^140.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
